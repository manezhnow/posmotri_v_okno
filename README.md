# Посмотри в окно

Интерактивный веб-сервис для поиска и просмотра видео с видами из окон разных городов в разное время суток.

## 📋 Описание проекта

Проект представляет собой одностраничное веб-приложение, которое позволяет пользователям:
- Искать видео по названию города
- Фильтровать видео по времени суток (утро, день, вечер, ночь)
- Просматривать видео в встроенном плеере

## 🏗️ Структура проекта

```
index.html             # Главный HTML файл приложения
styles/
├── style.css          # Основные стили приложения
├── preloader.css      # Стили для индикатора загрузки
└── error.css          # Стили для сообщений об ошибках
scripts/
└── script.js          # Основной JavaScript файл с логикой приложения
fonts/
└── fonts.css          # Подключение пользовательских шрифтов
```

## 🔧 Технологический стек

- **HTML5**: Семантическая разметка страницы
- **CSS3**: Современные техники стилизации (Grid, Flexbox, анимации)
- **JavaScript (Vanilla)**: Логика приложения без использования фреймворков
- **API**: REST API для получения видеоконтента

## 🚀 Установка и запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/manezhnow/posmotri_v_okno.git
```

2. Откройте файл `index.html` в браузере:
```bash
cd posmotri_v_okno
open index.html
```

Или используйте локальный сервер (например, Live Server в VS Code).

## 👤 Автор

manezhnow
